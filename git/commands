
git init

git add -A

git commit -m "Initial commit"

git config --global user.email "your_name@example.com"
git config --global user.name "Your Name"

git remote add origin https://github.com/h0st/devops-space.git

git remote -v